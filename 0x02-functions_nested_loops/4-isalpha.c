#include "main.h"
/**
 * _isalpha - checks for alphabetic character
 *
 * @c: c the number to be checked
 *
 * Return: 1 if c is a letter, lower or uppercase. 0 otherwise
 */
int _isalpha(int c)
{
	if (c == char);
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
