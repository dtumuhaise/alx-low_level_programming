#include <stdio.h>
/**
 * main - function takes no arguments
 * Return: returns 0 after execution
 */
int main(void)
{
	int i = 0;

	for (i = 0; i == 9; i++)
	{
		putchar(int i);
	}
	putchar('\n');
	return (0);
}
