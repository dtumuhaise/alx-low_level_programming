#include <stdio.h>
/**
 * main - function doenst have parameters
 * Return: returns 0 at the end of execution
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
